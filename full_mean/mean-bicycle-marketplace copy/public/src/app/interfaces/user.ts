export interface User {
    first_name ?: String,
    last_name ?: String,
    email ?: String,
    password ?: String,
    passwordconfirm ?: String
}
// This is our user interface
// The ?: means not required
